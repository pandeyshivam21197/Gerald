import React from 'react';
import Icon from '../../components/Icon';
import {IconName} from '../../components/Icon/constants';

export const DrawerIcon = props => {
  return <Icon {...props} name={IconName.phone} size={30} />;
};
